/**
 * Domain Event Base Class
 * Re-exports from aggregate-root for consistency
 */

// Re-export the interface from aggregate-root to maintain consistency
export type { DomainEvent } from './aggregate-root';